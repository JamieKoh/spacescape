using System;

namespace CustomProgram{

    /// <summary>
    /// This is an enum of all the enemy types
    /// </summary>
    public enum Type{
        asteroid,
        debrisMites,
        Holdians,
        Monpai
    }
}